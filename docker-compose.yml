# Use postgres/example user/password credentials
version: '3.1'

services:
    lazydb:
        image: postgres
        restart: always
        environment:
            POSTGRES_DB: lazydb
            POSTGRES_USER: lazyuser
            POSTGRES_PASSWORD: lazysecret
        ports:
            - 5432:5432
